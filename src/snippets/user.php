<?php

if(!class_exists('User')) Components::require('Notification');

class User {

    // This widget is used to handle user related tasks,
    // such as sign-in, sign-out, sign-up, etc.

    public static function signIn(): void {

    }

}